import 'package:flutter/material.dart';

// TODO: implementasikan sesuai Bugarin_PRD_Mobile.md bab 3.6 — card per progress_cycle
// (expand lazy-load daily logs, lihat family provider di skill flutter-riverpod-patterns).
class RiwayatScreen extends StatelessWidget {
  const RiwayatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Riwayat'));
  }
}
